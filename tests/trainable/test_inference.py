


"""Auto-generated test stubs for trainable.inference."""

import pytest
from unittest.mock import MagicMock, patch
import trainable.inference


def test_load_trained_instrument():
    """Test for load_trained_instrument."""
    # -- Setup --
    experiment_id_or_path = ""
    # mock_ExperimentManager = MagicMock(return_value=None)
    # mock_join = MagicMock(return_value=None)
    # mock_get = MagicMock(return_value=None)
    # mock_get_trainable_instrument = MagicMock(return_value=None)
    # mock_wraps = MagicMock(return_value=None)
    # mock_exists = MagicMock(return_value=None)
    # mock_isdir = MagicMock(return_value=None)
    # mock_get_experiment = MagicMock(return_value=None)
    # mock_ValueError = MagicMock(return_value=None)
    # mock_FileNotFoundError = MagicMock(return_value=None)
    # mock_load = MagicMock(return_value=None)
    # mock_ParameterContext = MagicMock(return_value=None)
    # mock_original_blueprint = MagicMock(return_value=None)
    # mock_Experiment = MagicMock(return_value=None)
    # mock_MockExperiment = MagicMock(return_value=None)
    # mock_safe_load = MagicMock(return_value=None)
    # -- Act --
    result = trainable.inference.load_trained_instrument(experiment_id_or_path)
    # -- Assert --
    assert result == None
