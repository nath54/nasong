


"""Auto-generated test stubs for trainable.note_detection.create."""

import pytest
from unittest.mock import MagicMock, patch
import trainable.note_detection.create


def test_create_note_detector():
    """Test for create_note_detector."""
    # -- Setup --
    config = None
    # mock_strip = MagicMock(return_value=None)
    # mock_asdict = MagicMock(return_value=None)
    # mock_LegacyDetector = MagicMock(return_value=None)
    # mock_replace = MagicMock(return_value=None)
    # mock_BasicPitchDetector = MagicMock(return_value=None)
    # mock_LibrosaDetector = MagicMock(return_value=None)
    # mock_lower = MagicMock(return_value=None)
    # mock_TorchCrepeDetector = MagicMock(return_value=None)
    # mock_AudioFluxDetector = MagicMock(return_value=None)
    # mock_OnnxCrepeDetector = MagicMock(return_value=None)
    # mock_ValueError = MagicMock(return_value=None)
    # -- Act --
    result = trainable.note_detection.create.create_note_detector(config)
    # -- Assert --
    assert result == None
