


"""Auto-generated test stubs for instruments.bowed_strings."""

import pytest
from unittest.mock import MagicMock, patch
import instruments.bowed_strings


def test_Violin():
    """Test for Violin."""
    # -- Setup --
    time = None
    frequency = 0.0
    start_time = 0.0
    duration = 0.0
    amplitude = 0.0
    vibrato_rate = 0.0
    vibrato_depth = 0.0
    sample_rate = 0
    # mock_LFO = MagicMock(return_value=None)
    # mock_ADSR2 = MagicMock(return_value=None)
    # mock_Product = MagicMock(return_value=None)
    # mock_Sum = MagicMock(return_value=None)
    # mock_WhiteNoise = MagicMock(return_value=None)
    # mock_c = MagicMock(return_value=None)
    # mock_Formant = MagicMock(return_value=None)
    # mock_append = MagicMock(return_value=None)
    # mock_sqrt = MagicMock(return_value=None)
    # mock_uniform = MagicMock(return_value=None)
    # mock_Sin = MagicMock(return_value=None)
    # -- Act --
    result = instruments.bowed_strings.Violin(time, frequency, start_time, duration, amplitude, vibrato_rate, vibrato_depth, sample_rate)
    # -- Assert --
    assert result == None

def test_Cello():
    """Test for Cello."""
    # -- Setup --
    time = None
    frequency = 0.0
    start_time = 0.0
    duration = 0.0
    amplitude = 0.0
    vibrato_rate = 0.0
    vibrato_depth = 0.0
    sample_rate = 0
    # mock_LFO = MagicMock(return_value=None)
    # mock_ADSR2 = MagicMock(return_value=None)
    # mock_Product = MagicMock(return_value=None)
    # mock_Sum = MagicMock(return_value=None)
    # mock_WhiteNoise = MagicMock(return_value=None)
    # mock_c = MagicMock(return_value=None)
    # mock_Formant = MagicMock(return_value=None)
    # mock_append = MagicMock(return_value=None)
    # mock_sqrt = MagicMock(return_value=None)
    # mock_uniform = MagicMock(return_value=None)
    # mock_Sin = MagicMock(return_value=None)
    # -- Act --
    result = instruments.bowed_strings.Cello(time, frequency, start_time, duration, amplitude, vibrato_rate, vibrato_depth, sample_rate)
    # -- Assert --
    assert result == None
