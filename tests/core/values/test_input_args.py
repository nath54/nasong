


"""Auto-generated test stubs for core.values.input_args."""

import pytest
from unittest.mock import MagicMock, patch
import core.values.input_args


def test_input_args_to_values():
    """Test for input_args_to_values."""
    # -- Setup --
    values = ()
    # mock_Constant = MagicMock(return_value=None)
    # mock_append = MagicMock(return_value=None)
    # -- Act --
    result = core.values.input_args.input_args_to_values(values)
    # -- Assert --
    assert result == []
